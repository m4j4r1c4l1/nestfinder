{
    "name": "nestfinder-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "npm install && cd client && npm install && npm run build && cd ../admin && npm install && npm run build && cd ../server && npm install",
        "start": "cd server && npm start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}